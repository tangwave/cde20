# 依赖 / 虚拟环境
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# 本地密钥 / 环境
.env
llm_config.json
llm_presets.json
*.local

# 大文件：kb.sqlite 约 212MB，超过 GitHub 100MB 单文件限制。
# 默认用「构建时下载」（scripts/download_kb.sh + Render 环境变量 KB_SQLITE_URL）。
# 若改用 Git LFS，请删掉下面这行并在 .gitattributes 中启用 LFS 规则（见 README）。
# 整个 00_索引 目录（含本机 junction 符号链接、kb.sqlite、索引文件）均不入库；
# 部署时通过挂载卷 / KB_SQLITE_URL 下载 / 手动放入 kb.sqlite。
00_索引

# 系统文件
.DS_Store
Thumbs.db
backend.log
