fastapi>=0.110
uvicorn[standard]>=0.27
# kb_query.py -> kb_common.py 依赖
requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.11
# 用户登录模块（api/auth.py）
#   psycopg2-binary：Postgres 驱动。未设 DATABASE_URL 时自动回退 SQLite，本地开发可不装。
#   bcrypt：口令散列。若不可用则自动回退到标准库 PBKDF2-HMAC-SHA256(600k 轮)。
psycopg2-binary>=2.9
bcrypt>=4.0
