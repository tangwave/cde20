# Git LFS 配置（可选）：改为「LFS 入库 kb.sqlite」时启用。
#
# 使用 LFS 方式：
#   1. 取消下方注释（删掉行首的 #）
#   2. 从 .gitignore 删掉 00_索引/kb.sqlite 那一行
#   3. git lfs install && git add 00_索引/kb.sqlite && git push
#   4. Render 克隆时会自动拉取 LFS 大文件（需 Render 构建环境支持 git-lfs）
#
# 默认推荐「构建时下载」方案（见 scripts/download_kb.sh），故本行默认注释。
# 00_索引/kb.sqlite filter=lfs diff=lfs merge=lfs -text
